name: Conflict check

on:
  push:
    branches: [dev, content]
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  conflict:
    name: Check for merge conflicts
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          fetch-depth: 0

      - uses: actions/github-script@v8
        with:
          script: |
            const dirtyLabel = "has-conflicts";
            const commentOnDirty = `This PR has merge conflicts with the target branch and cannot be merged until they are resolved.

            Please rebase or merge the target branch into your branch and resolve the conflicts locally, then push the updated branch.`;
            const commentOnClean = "Merge conflicts have been resolved. Thank you!";
            const { owner, repo } = context.repo;

            async function candidatePulls() {
              if (context.payload.pull_request) {
                return [context.payload.pull_request];
              }

              const base = context.ref.replace("refs/heads/", "");
              return github.paginate(github.rest.pulls.list, {
                owner,
                repo,
                state: "open",
                base,
                per_page: 100,
              });
            }

            async function pullWithMergeableState(candidate) {
              let latest = candidate;

              for (let attempt = 0; attempt < 10; attempt += 1) {
                const { data: pull } = await github.rest.pulls.get({
                  owner,
                  repo,
                  pull_number: candidate.number,
                });
                latest = pull;

                if (pull.mergeable !== null && pull.mergeable_state !== "unknown") {
                  return pull;
                }

                await new Promise((resolve) => setTimeout(resolve, 3000));
              }

              const headRef = `refs/remotes/pull/${candidate.number}/head`;
              const fetchResult = await exec.getExecOutput(
                "git",
                [
                  "fetch",
                  "--force",
                  "--no-tags",
                  "origin",
                  `pull/${candidate.number}/head:${headRef}`,
                ],
                { ignoreReturnCode: true, silent: true },
              );

              if (fetchResult.exitCode !== 0) {
                core.warning(
                  `Could not fetch PR #${candidate.number} for local conflict detection`,
                );
                return null;
              }

              const mergeResult = await exec.getExecOutput(
                "git",
                ["merge-tree", "--write-tree", latest.base.sha, headRef],
                { ignoreReturnCode: true, silent: true },
              );

              if (mergeResult.exitCode > 1) {
                core.warning(
                  `Could not determine mergeability for PR #${candidate.number}`,
                );
                return null;
              }

              return {
                ...latest,
                mergeable_state: mergeResult.exitCode === 1 ? "dirty" : "clean",
              };
            }

            async function updateConflictLabel(pull) {
              const current = await pullWithMergeableState(pull);
              if (current === null) {
                return;
              }

              const labels = current.labels.map((label) => label.name);
              const hasDirtyLabel = labels.includes(dirtyLabel);
              const isDirty = current.mergeable_state === "dirty";

              if (isDirty && !hasDirtyLabel) {
                await github.rest.issues.addLabels({
                  owner,
                  repo,
                  issue_number: current.number,
                  labels: [dirtyLabel],
                });
                await github.rest.issues.createComment({
                  owner,
                  repo,
                  issue_number: current.number,
                  body: commentOnDirty,
                });
              }

              if (!isDirty && hasDirtyLabel) {
                await github.rest.issues.removeLabel({
                  owner,
                  repo,
                  issue_number: current.number,
                  name: dirtyLabel,
                });
                await github.rest.issues.createComment({
                  owner,
                  repo,
                  issue_number: current.number,
                  body: commentOnClean,
                });
              }
            }

            for (const pull of await candidatePulls()) {
              await updateConflictLabel(pull);
            }
