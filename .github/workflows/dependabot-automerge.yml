name: Dependabot auto-merge

on:
  pull_request_target:
    types: [opened, reopened, synchronize]

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  automerge:
    uses: nicxe/.github/.github/workflows/reusable-dependabot-automerge.yml@af7822ccdd4d8e2f6bf6ea3d46e58988f6d6275d
